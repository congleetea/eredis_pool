{application, eredis_pool,
 [
  {description, ""},
  {vsn, "1.1"},
  {registered, []},
  {applications, [
                  kernel,
                  stdlib
                 ]},
  {mod, { eredis_pool_app, []}},
  {env, [
         {pools, [
                  {default, [
                             {size, 10},
                             {max_overflow, 20}
                            ], []},
                  {pool1, [
                           {size, 30},
                           {max_overflow, 20}
                          ], [
                              {host, "192.168.31.120"},
                              {port, 6379}
                             ]},
                  {pool2, [
                           {size, 20},
                           {max_overflow, 20}
                          ], [
                              {host, "192.168.31.120"},
                              {port, 6379},
                              {database, "mqtt"},
                              {password, "mqtt"},
                              {reconnect_sleep, 100}
                             ]}
                 ]}
        ]}
]}.
